spring.application.name=glpayroll
#spring.main.allow-bean-definition-overriding=true
spring.datasource.url=jdbc:postgresql://localhost:5432/glpayrolldomain
spring.datasource.username=postgres
spring.datasource.password=Danie40g
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Set the root logging level to WARN to reduce noise from libraries
logging.level.root=ERROR

# Set the logging level for your application's package to DEBUG
#logging.level.com.example.demo=DEBUG 

# Writes logs to a file named 'app.log' in the current directory
logging.file.name=app.log

# OR writes 'spring.log' to a specified directory
#logging.file.path=/var/log/myapp


# Hibernate settings (optional, but useful)
spring.jpa.hibernate.ddl-auto=update

spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

springdoc.swagger-ui.path=/swagger-ui-rest.html
springdoc.swagger-ui.operationsSorter=method

spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=100

spring.data.web.pageable.page-parameter=page
spring.data.web.pageable.size-parameter=size
spring.data.web.sort.sort-parameter=sort

# Max size for a single file
spring.servlet.multipart.max-file-size=10MB
# Max size for the entire multipart request
spring.servlet.multipart.max-request-size=50MB
# Enable multipart support
spring.servlet.multipart.enabled=true

app.jwtsecret=8fhgeigayuf64oq8ltw36pi84wl87trf8afpw7ty934t7fwaeGTFyugfldlsjfdglja
app.clientUrl=http://localhost:4200

